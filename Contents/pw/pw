cd ~/sh/app/paper_write

if [[ $1 == -h ]]; then
        cat help
elif [[ $1 == -p ]]; then
    if [[ ! -e ./configs/$2 ]]; then
        echo $2 "isn't an existing config file"
        exit 1
    fi
    export config_file=$2
    ./app "${@:3}"
else
    ./app $@
fi
