#!/bin/bash
# ls files in iCloud

# 定义源路径和目标路径
TARGET_PATH="/Users/shijitao/Library/Mobile Documents/com~apple~CloudDocs/"
var=${@: -1}

if [[ $var == -* ]]; then
    ls $@ "$TARGET_PATH"
else
    ls ${@:1:$#-1} "$TARGET_PATH"${@: -1}
fi
